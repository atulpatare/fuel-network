schema {
    query: QueryRoot
}

type QueryRoot {
    count: Count
    increment: Increment
    decrement: Decrement
}

type Increment {
    id: ID!
    count: Int8!
}

type Decrement {
    id: ID!
    count: Int8!
}

type Count {
    id: ID!
    count: Int8!
}
